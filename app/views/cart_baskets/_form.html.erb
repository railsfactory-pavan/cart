<%= form_with(model: cart_basket, local: true) do |form| %>
  <% if cart_basket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cart_basket.errors.count, "error") %> prohibited this cart_basket from being saved:</h2>

      <ul>
      <% cart_basket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
